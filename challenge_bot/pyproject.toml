[project]
name = "challenge-bot"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "python-telegram-bot>=22.0,<23",
    "supabase>=2.31.0",
    "python-dotenv>=1.0.1",
    "streamlit>=1.58.0",
]

[dependency-groups]
dev = ["pytest>=8.0"]
