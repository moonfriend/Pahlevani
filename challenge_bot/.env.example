# For LOCAL dev, see run.sh + ../env/challenge_bot.env.example instead — this
# file is for standalone deployments (e.g. a VPS via systemd, see DEPLOY.md)
# with no access to the local admin creds vault, so it needs all three
# values self-contained.
TELEGRAM_BOT_TOKEN=
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
